# Instalação completa (núcleo + transcrição local em Python puro).
# Alternativa com extras seletivos, via pyproject.toml:
#   pip install -e .                     # sem torch/whisper (usa whisper.cpp)
#   pip install -e ".[transcription]"
#
# Faixas com teto no próximo major evitam quebras silenciosas em `pip install`.

# Núcleo
pydantic>=2.0,<3.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
edge-tts>=7.0,<8.0

# Transcrição local com openai-whisper (pesado, ~2-3 GB).
# Dispensável se você usar whisper.cpp, que é o caminho do app desktop.
openai-whisper>=20230918
torch>=2.0,<3.0
